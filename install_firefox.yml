---

- hosts: all
  become: true
  tasks:

  - name: install htop
    apt:
      name: htop
      state: latest

  - name: Remove-htop
    apt:
      name: 
      state: 

  - name: Update-repo
    apt:
      update_cache: yes

  - name: Task-Name(Install Firefox)
    apt:
      name: firefox
      state: latest
